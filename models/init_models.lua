--require your models and pass then as key is the name of the table and the value is the model.
-- example local car_model = require('models.car_model')
return {
    -- ['users'] = car_model.User,
    -- ['profile'] = car_model.Profile
}